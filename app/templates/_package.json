{
  "name": "<% _.slugify(appname) %>",
  "version": "0.0.0",
  "description": "Yo Milagro Email",
  "dependencies": {},
  "devDependencies": {
    "mocha": "*",
    "grunt": "^0.4.4",<% if (autoPre) { %>
    "grunt-contrib-htmlmin": "^0.2.0",
    "grunt-load-tasks": "~0.2.0",
    "time-grunt": "~0.2.8"
  },
  "engines": {
    "node": ">=0.10.0"
  },
}